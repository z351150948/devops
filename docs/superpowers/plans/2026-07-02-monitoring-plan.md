# 监控扩展（主机 + 数据库）— B 轮实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 在现有 K8sManage.vue / ContainerManage.vue 工作台加 tab，对 `ops.Host` 拉 CPU/Mem/Disk/网络/Load/Uptime on-demand 指标，对 `sqlaudit.DataSource` 拉 MySQL+Redis+MongoDB 连接/QPS/缓存/复制 on-demand 指标。

**架构：** 新建独立 `monitoring/` app（参照 cmdb / aiops 模式），复用 `ops.Host` + `sqlaudit.DataSource` 作数据源；按需采集（无后台任务、无历史表），仅 `ProbeAudit` 表存最近一次采集状态。

**技术栈：** Django 5.2 + DRF + Channels（已装） / paramiko 3.4（已装）/ pymysql 1.1（已装）/ pymongo 4.8（已装）/ redis-py 5+（**待任务 1 确认**）/ Vue 3.5 + Vite 6 + Element Plus（已装）。

**配套 spec：** [specs/2026-07-02-monitoring-design.md](../specs/2026-07-02-monitoring-design.md)（commit `a3a46c8` on `test`）

---

## 文件结构

### 新增文件

| 路径 | 职责 |
| --- | --- |
| `backend/monitoring/__init__.py` | Django app 入口（含 `default_app_config`） |
| `backend/monitoring/apps.py` | `MonitoringConfig(AppConfig)`，设置 `verbose_name` + `default_auto_field` |
| `backend/monitoring/urls.py` | 4 个 endpoint 路由表 |
| `backend/monitoring/models.py` | `ProbeAudit` 单表（最近一次采集状态） |
| `backend/monitoring/migrations/0001_initial.py` | 0001 初始 migration（auto-generated by makemigrations） |
| `backend/monitoring/serializers.py` | `HostProbeRequestSerializer` / `HostProbeResultSerializer` / `DatabaseProbeResultSerializer` |
| `backend/monitoring/api.py` | 4 个 APIView 类 + 私有 `run_host_probe` / `run_database_probe` |
| `backend/monitoring/services/__init__.py` | 服务层包入口（空文件） |
| `backend/monitoring/services/host_probe.py` | 主机 SSH 采集（复用 `ops.host_tasks.open_ssh_client` + `execute_remote_command`） |
| `backend/monitoring/services/mysql_probe.py` | MySQL `show global status` + `show slave status` 解析 |
| `backend/monitoring/services/redis_probe.py` | Redis `info()` 解析（**D-2b 降级时**本文件标记 skip） |
| `backend/monitoring/services/mongo_probe.py` | MongoDB `serverStatus()` 解析 |
| `backend/monitoring/tests/__init__.py` | 测试包入口 |
| `backend/monitoring/tests/test_host_probe.py` | 主机采集服务测试（mock paramiko） |
| `backend/monitoring/tests/test_mysql_probe.py` | MySQL 采集服务测试（mock pymysql） |
| `backend/monitoring/tests/test_redis_probe.py` | Redis 采集服务测试（mock redis-py，**D-2b 降级时**改 `pytest.skip`） |
| `backend/monitoring/tests/test_mongo_probe.py` | MongoDB 采集服务测试（mock pymongo） |
| `backend/monitoring/tests/test_api.py` | API 集成测试（RBAC + 端点 + 单 host 失败不中断） |
| `frontend/src/api/modules/monitoring.js` | 4 个前端 API 包装函数 |

### 修改文件

| 路径 | 改动 |
| --- | --- |
| `backend/sxdevops/settings.py` | `INSTALLED_APPS` 末尾追加 `'monitoring.apps.MonitoringConfig'` |
| `backend/sxdevops/urls.py` | 末尾追加 `path('api/monitoring/', include('monitoring.urls'))` |
| `backend/rbac/registry.py` | `PERMISSION_DEFINITIONS` 追加 2 项 + `BUILTIN_ROLES['ops-admin']` 追加 2 项 |
| `backend/requirements.txt` | **D-2b 降级时不动**；D-2 正常时追加 `redis>=5.0` |
| `frontend/src/views/K8sManage.vue` | `mainTabs` 数组末尾追加 `hosts-monitor` tab + 4 stat card + host 列表 |
| `frontend/src/views/ContainerManage.vue` | `mainTabs` 数组末尾追加 `db-monitor` tab + 4 stat card + db 列表 |

### 严格不改

- `backend/sxdevops/**` Python 包名（L1 锁定）
- `ops.Host` model 字段 / `ops.Host.ssh_password` 明文
- `sqlaudit.DataSource` model 字段
- `ops/observability_views.py` 现有 ViewSet
- `K8sManage.vue` 现有 8 个 tab / `ContainerManage.vue` 现有 3 个 tab
- `frontend/src/router/index.js`（不新增路由）
- `scripts/rebrand/{sxdevops_to_autotoll,verify_rebrand}.py`（A 轮已闭环）
- `docs/superpowers/{specs,plans}/**` 元文档自身

---

## 阶段 A：环境核查 + monitoring app 骨架

### 任务 1：环境核查 + D-2 决策

**文件：**
- 探测：`bash`（无文件改动）
- 修改（仅 D-2b 降级时）：`backend/requirements.txt`

- [ ] **步骤 1：探测 redis-py 状态**

```bash
cd devops
python -c "import redis; print('redis-py available:', redis.__version__)" 2>&1
pip show redis 2>&1 | head -5
```

预期三选一：
- **(A) `redis-py available: 5.x.x`**：D-2 正常走，3 个 DB 都支持。
- **(B) `ModuleNotFoundError: No module named 'redis'`**：D-2 降级为 D-2b（仅 MySQL + MongoDB），redis_probe.py 标 `pytest.skip`。
- **(C) `redis-py 3.x.x` 老版本**：D-2 走但 plan 任务 6 适配 `info(section='all')` API。

- [ ] **步骤 2：探测 INSTALLED_APPS 末尾位置**

```bash
grep -n "'ops.apps.OpsConfig'\|'aiops.apps.AiopsConfig'\|'eventwall'" backend/sxdevops/settings.py
```

预期：找到最后一项 local app 位置，planning 在其后追加 `'monitoring.apps.MonitoringConfig'`。

- [ ] **步骤 3：探测 sqlaudit.DataSource connection_params 字段 schema**

```bash
cd devops
python -c "
import os
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'sxdevops.settings')
import django; django.setup()
from sqlaudit.models import DataSource
print('db_type choices:', DataSource._meta.get_field('db_type').choices)
print('connection_params field type:', type(DataSource._meta.get_field('connection_params')).__name__)
" 2>&1
```

预期输出 `db_type choices` 列表含 `mysql/redis/mongo`（或类似），`connection_params` 为 `JSONField`。记录这些值，planning 任务 5-8 service 层读取 `connection_params` 时按此 schema。

- [ ] **步骤 4：D-2 决策落地**

按步骤 1 结果：

- (A) D-2 正常：无需改 `requirements.txt`（redis-py 已被传递依赖装上），继续任务 2。
- (B) D-2b 降级：保持 `requirements.txt` 不动；任务 6 写 `redis_probe.py` 但测试标 `pytest.skip`；任务 8 跳过 Redis 路径。
- (C) 适配老 API：步骤 1 备注 redis-py 版本，任务 6 适配。

- [ ] **步骤 5：探测 ops.host_tasks 复用点**

```bash
grep -n "open_ssh_client\|execute_remote_command\|_metrics_shell_command" backend/ops/host_tasks.py
```

预期：找到 3 个函数（行号记下），任务 4 直接 `from ops.host_tasks import open_ssh_client, execute_remote_command` 复用，不重写 SSH 5 步走。

- [ ] **步骤 6：记录到 plan 注释**

把步骤 1-5 结果写到 `docs/superpowers/plans/2026-07-02-monitoring-plan.md` 末尾的"实施记录"段（如该文件不存在，下一次 commit 时新增）。

- [ ] **步骤 7：Commit（如 D-2b）**

仅 D-2b 降级时：
```bash
cd devops
# 实际 D-2b 不改 requirements.txt，所以这个 commit 通常无文件改动，跳过。
```

如 D-2b 实施中需要新建 placeholder（例如 `backend/monitoring/PLACEHOLDER.md`），commit 一次：
```bash
git add backend/monitoring/PLACEHOLDER.md
git commit -m "chore(monitoring): D-2b 降级 placeholder（redis-py 不可用）"
```

预期：本任务通常不产生 commit（D-2 正常或 D-2b 但 requirements.txt 不动）。

---

### 任务 2：monitoring app 骨架 + INSTALLED_APPS 注册

**文件：**
- 创建：`backend/monitoring/__init__.py`
- 创建：`backend/monitoring/apps.py`
- 创建：`backend/monitoring/urls.py`（含占位路由，任务 8 填实）
- 修改：`backend/sxdevops/settings.py` 末尾追加 `'monitoring.apps.MonitoringConfig'`
- 修改：`backend/sxdevops/urls.py` 末尾追加 `path('api/monitoring/', include('monitoring.urls'))`

- [ ] **步骤 1：写 `backend/monitoring/__init__.py`**

```python
default_app_config = 'monitoring.apps.MonitoringConfig'
```

- [ ] **步骤 2：写 `backend/monitoring/apps.py`**

```python
from django.apps import AppConfig

class MonitoringConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'monitoring'
    verbose_name = '监控'
```

- [ ] **步骤 3：写 `backend/monitoring/urls.py`（占位，任务 8 填实）**

```python
from django.urls import path

urlpatterns = [
    # 任务 8 填入：
    # path('hosts/probe/', views.HostProbeBulkView.as_view(), name='monitoring-hosts-probe-bulk'),
    # path('hosts/<int:host_id>/probe/', views.HostProbeSingleView.as_view(), name='monitoring-hosts-probe-single'),
    # path('databases/probe/', views.DatabaseProbeBulkView.as_view(), name='monitoring-databases-probe-bulk'),
    # path('databases/<int:ds_id>/probe/', views.DatabaseProbeSingleView.as_view(), name='monitoring-databases-probe-single'),
]
```

- [ ] **步骤 4：注册 monitoring app 到 INSTALLED_APPS**

修改 `backend/sxdevops/settings.py`，在 `'eventwall',`（最后一项 local app）后追加：
```python
    'eventwall',
    'monitoring.apps.MonitoringConfig',
```

- [ ] **步骤 5：注册 monitoring URL 到主 urls**

修改 `backend/sxdevops/urls.py`，找到 `urlpatterns = [` 列表末尾（`path('', include('eventwall.urls')),` 之类），追加：
```python
    path('api/monitoring/', include('monitoring.urls')),
```

- [ ] **步骤 6：验证 Django 检查通过**

```bash
cd devops
python manage.py check 2>&1 | tail -10
```

预期：`System check identified no issues (0 silenced).`

- [ ] **步骤 7：Commit**

```bash
cd devops
git add backend/monitoring/ backend/sxdevops/settings.py backend/sxdevops/urls.py
git commit -m "feat(monitoring): 新建 monitoring app 骨架 + INSTALLED_APPS + urls 注册"
```

预期 1 commit，working tree 干净。

---

### 任务 3：ProbeAudit model + 0001 migration

**文件：**
- 创建：`backend/monitoring/models.py`
- 创建：`backend/monitoring/migrations/__init__.py`
- 创建：`backend/monitoring/migrations/0001_initial.py`（auto-generated）

- [ ] **步骤 1：写 `backend/monitoring/models.py`**

```python
from django.db import models

class ProbeAudit(models.Model):
    """最近一次采集成功/失败时间（不存历史指标，决策 1）。"""

    TARGET_HOST = 'host'
    TARGET_DATABASE = 'database'
    TARGET_KIND_CHOICES = [
        (TARGET_HOST, '主机'),
        (TARGET_DATABASE, '数据库'),
    ]

    STATUS_OK = 'ok'
    STATUS_ERROR = 'error'
    STATUS_TIMEOUT = 'timeout'
    STATUS_MISCONFIGURED = 'misconfigured'
    STATUS_CHOICES = [
        (STATUS_OK, '成功'),
        (STATUS_ERROR, '错误'),
        (STATUS_TIMEOUT, '超时'),
        (STATUS_MISCONFIGURED, '凭据缺失'),
    ]

    target_kind = models.CharField('目标类型', max_length=16, choices=TARGET_KIND_CHOICES)
    target_id = models.IntegerField('目标 ID（ops.Host.id 或 sqlaudit.DataSource.id）')
    last_status = models.CharField('最近状态', max_length=16, choices=STATUS_CHOICES)
    last_error = models.TextField('最近错误', blank=True, default='')
    last_duration_ms = models.IntegerField('最近耗时 ms', default=0)
    last_collected_at = models.DateTimeField('最近采集时间', auto_now=True)

    class Meta:
        db_table = 'monitoring_probe_audit'
        verbose_name = '采集审计'
        verbose_name_plural = '采集审计'
        unique_together = ('target_kind', 'target_id')
        indexes = [models.Index(fields=['target_kind', 'last_collected_at'])]
```

- [ ] **步骤 2：写 `backend/monitoring/migrations/__init__.py`**

```python
# empty file
```

- [ ] **步骤 3：生成 0001_initial migration**

```bash
cd devops
python manage.py makemigrations monitoring 2>&1 | tail -10
ls backend/monitoring/migrations/
```

预期：生成 `0001_initial.py` 包含 `ProbeAudit` 表 DDL（unique_together + index）。

- [ ] **步骤 4：本地 migrate（不入 git 也不入 DB，dry-run）**

```bash
cd devops
python manage.py sqlmigrate monitoring 0001 2>&1 | tail -20
```

预期：输出 `CREATE TABLE` SQL。**不**实际跑 migrate（开发数据库状态由维护者决定）。

- [ ] **步骤 5：Commit**

```bash
cd devops
git add backend/monitoring/models.py backend/monitoring/migrations/
git commit -m "feat(monitoring): ProbeAudit model + 0001 initial migration"
```

预期 1 commit。

---

## 阶段 B：services 采集层

### 任务 4：services/host_probe.py（主机 SSH 采集）

**文件：**
- 创建：`backend/monitoring/services/__init__.py`（空）
- 创建：`backend/monitoring/services/host_probe.py`

- [ ] **步骤 1：写 `backend/monitoring/services/__init__.py`**

```python
# 服务层入口（空）
```

- [ ] **步骤 2：写测试 `backend/monitoring/tests/test_host_probe.py`**

```python
"""主机采集服务测试。"""
import socket
from unittest.mock import patch, MagicMock

from django.test import TestCase

from monitoring.services.host_probe import (
    probe_host,
    ProbeResult,
    ProbeStatus,
)


class _FakeSsh:
    """模拟 paramiko.SSHClient + exec_command。"""
    def __init__(self, output_map):
        self._output_map = output_map  # {cmd_substr: output_str}
        self._closed = False

    def set_missing_host_key_policy(self, _): pass
    def connect(self, **kwargs): pass

    def exec_command(self, cmd, timeout=None):
        # 根据 cmd 找匹配的 output
        for substr, output in self._output_map.items():
            if substr in cmd:
                chan = MagicMock()
                chan.recv_exit_status = MagicMock(return_value=0)
                stdin = MagicMock()
                stdout = MagicMock()
                stdout.read = MagicMock(return_value=output.encode('utf-8'))
                stdout.channel = chan
                stderr = MagicMock()
                stderr.read = MagicMock(return_value=b'')
                return stdin, stdout, stderr
        chan = MagicMock()
        chan.recv_exit_status = MagicMock(return_value=1)
        stdin = MagicMock()
        stdout = MagicMock()
        stdout.read = MagicMock(return_value=b'')
        stdout.channel = chan
        stderr = MagicMock()
        stderr.read = MagicMock(return_value=b'command not found'.encode('utf-8'))
        return stdin, stdout, stderr

    def close(self):
        self._closed = True


class HostProbeOk(TestCase):
    @patch('monitoring.services.host_probe.open_ssh_client')
    def test_probe_normal_host_returns_metrics(self, mock_open):
        # 模拟 /proc/loadavg + free -m + df + uptime 输出
        mock_open.return_value = _FakeSsh({
            '/proc/loadavg': '0.32 0.28 0.31 1/123 4567\n',
            'free -m': '              total        used        free\nMem:           16000        4800       11200\n',
            'df -BG': 'Filesystem     1G-blocks  Used Available Use% Mounted on\n/dev/sda1            100G    45G    55G  45% /\n',
            'uptime': ' 1234567.89 12345.67\n',
            '/proc/net/dev': 'eth0: 1234567 0 0 0 0 0 0 0 7654321 0 0 0 0 0 0 0\n',
        })

        from ops.models import Host
        host = Host.objects.create(
            hostname='node-01', ip_address='10.10.30.100',
            ssh_port=22, ssh_user='root', ssh_password='x',
        )

        result = probe_host(host, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.OK)
        self.assertIsNone(result.error)
        self.assertIn('cpu', result.metrics)
        self.assertEqual(result.metrics['memory']['total_mb'], 16000)
        self.assertEqual(result.metrics['memory']['used_mb'], 4800)
        self.assertEqual(len(result.metrics['disk']), 1)
        self.assertEqual(result.metrics['disk'][0]['mount'], '/')
        self.assertEqual(result.metrics['uptime_seconds'], 1234567)


class HostProbeAuthError(TestCase):
    @patch('monitoring.services.host_probe.open_ssh_client')
    def test_probe_auth_error_returns_error_status(self, mock_open):
        import paramiko
        mock_open.side_effect = paramiko.AuthenticationException()

        from ops.models import Host
        host = Host.objects.create(
            hostname='node-02', ip_address='10.10.30.101',
            ssh_port=22, ssh_user='root', ssh_password='wrong',
        )

        result = probe_host(host, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.ERROR)
        self.assertIn('认证失败', result.error)


class HostProbeTimeout(TestCase):
    @patch('monitoring.services.host_probe.open_ssh_client')
    def test_probe_timeout_returns_timeout_status(self, mock_open):
        mock_open.side_effect = socket.timeout()

        from ops.models import Host
        host = Host.objects.create(
            hostname='node-03', ip_address='10.10.30.102',
            ssh_port=22, ssh_user='root', ssh_password='x',
        )

        result = probe_host(host, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.TIMEOUT)


class HostProbeMisconfigured(TestCase):
    @patch('monitoring.services.host_probe.open_ssh_client')
    def test_probe_missing_password_returns_misconfigured(self, mock_open):
        from ops.models import Host
        host = Host.objects.create(
            hostname='node-04', ip_address='10.10.30.103',
            ssh_port=22, ssh_user='root', ssh_password='',  # 空凭据
        )

        result = probe_host(host, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.MISCONFIGURED)
        # 验证 open_ssh_client 未被调用
        mock_open.assert_not_called()
```

- [ ] **步骤 3：跑测试验证失败**

```bash
cd devops
python manage.py test monitoring.tests.test_host_probe 2>&1 | tail -15
```

预期：FAIL，`No module named 'monitoring.services.host_probe'`。

- [ ] **步骤 4：写 `backend/monitoring/services/host_probe.py`**

```python
"""主机 SSH 采集（复用 ops.host_tasks 的 SSH 5 步走）。"""
import socket
import time
from dataclasses import dataclass, asdict
from typing import Optional

import paramiko

from ops.host_tasks import open_ssh_client, execute_remote_command


class ProbeStatus:
    OK = 'ok'
    ERROR = 'error'
    TIMEOUT = 'timeout'
    MISCONFIGURED = 'misconfigured'


@dataclass
class ProbeResult:
    status: str
    duration_ms: int = 0
    metrics: Optional[dict] = None
    error: Optional[str] = None

    def to_dict(self) -> dict:
        return asdict(self)


# 5 条采集命令 + 解析
_PROBE_COMMANDS = [
    ('cpu', 'cat /proc/loadavg'),
    ('memory', 'free -m'),
    ('disk', 'df -BG --output=source,size,used,avail,pcent,target'),
    ('uptime', 'cat /proc/uptime'),
    ('network', 'cat /proc/net/dev'),
]


def _parse_loadavg(output: str) -> dict:
    parts = output.strip().split()
    return {
        'load1': float(parts[0]) if len(parts) > 0 else 0.0,
        'load5': float(parts[1]) if len(parts) > 1 else 0.0,
        'load15': float(parts[2]) if len(parts) > 2 else 0.0,
    }


def _parse_memory(output: str) -> dict:
    # 输出: "              total        used        free\nMem:           16000        4800       11200"
    for line in output.splitlines():
        if line.startswith('Mem:'):
            parts = line.split()
            total_mb = int(parts[1])
            used_mb = int(parts[2])
            return {
                'total_mb': total_mb,
                'used_mb': used_mb,
                'usage_pct': round(used_mb / total_mb * 100, 1) if total_mb else 0.0,
            }
    return {'total_mb': 0, 'used_mb': 0, 'usage_pct': 0.0}


def _parse_disk(output: str) -> list:
    # 跳表头；每行: Filesystem 1G-blocks Used Available Use% Mounted on
    result = []
    lines = output.splitlines()[1:]  # skip header
    for line in lines:
        if not line.strip():
            continue
        parts = line.split()
        if len(parts) < 6:
            continue
        # Use% 形式如 "45%"
        use_pct = parts[4].rstrip('%')
        try:
            use_pct_num = int(use_pct)
        except ValueError:
            continue
        result.append({
            'mount': parts[5],
            'size_gb': int(parts[1].rstrip('G')) if parts[1].endswith('G') else 0,
            'used_pct': use_pct_num,
        })
    return result


def _parse_uptime(output: str) -> int:
    return int(float(output.split()[0]))


def _parse_network(output: str) -> dict:
    # 找 eth0 行（第一个非 lo 且非 sit/tun 等虚拟接口）
    rx_bytes = tx_bytes = 0
    for line in output.splitlines():
        if ':' not in line:
            continue
        iface_name = line.split(':', 1)[0].strip()
        if iface_name in ('lo', 'sit0', 'tunl0') or iface_name.startswith(('veth', 'docker', 'br-')):
            continue
        parts = line.split()
        if len(parts) < 9:
            continue
        rx_bytes += int(parts[1])
        tx_bytes += int(parts[9])
    return {'rx_bytes': rx_bytes, 'tx_bytes': tx_bytes}


def probe_host(host, timeout_seconds: int = 5) -> ProbeResult:
    """对一台主机做 on-demand 采集。失败时返回 status=error/timeout/misconfigured，**不抛异常**。"""
    started = time.monotonic()

    # 错配检查：密码 / 私钥为空
    if not (host.ssh_password or '').strip() and not (host.ssh_user or '').strip():
        return ProbeResult(
            status=ProbeStatus.MISCONFIGURED,
            duration_ms=int((time.monotonic() - started) * 1000),
            error='SSH 凭据缺失（ssh_user 与 ssh_password 均为空）',
        )

    try:
        client = open_ssh_client(host, timeout_seconds=timeout_seconds)
    except paramiko.AuthenticationException:
        return ProbeResult(
            status=ProbeStatus.ERROR,
            duration_ms=int((time.monotonic() - started) * 1000),
            error='SSH 认证失败',
        )
    except (paramiko.SSHException, socket.timeout, OSError) as exc:
        # socket.timeout 包含 connection timeout；OSError 包含 network unreachable
        if isinstance(exc, socket.timeout):
            return ProbeResult(
                status=ProbeStatus.TIMEOUT,
                duration_ms=int((time.monotonic() - started) * 1000),
                error=f'SSH 连接超时（>{timeout_seconds}s）',
            )
        return ProbeResult(
            status=ProbeStatus.ERROR,
            duration_ms=int((time.monotonic() - started) * 1000),
            error=f'SSH 不可达：{exc}',
        )

    try:
        metrics = {}
        for name, cmd in _PROBE_COMMANDS:
            try:
                _, output, _ = execute_remote_command(client, cmd, timeout_seconds=timeout_seconds)
            except (paramiko.SSHException, socket.timeout):
                metrics = {'partial': True, 'last_successful': name}
                # 一次失败不中断整次采集；标记为 timeout
                return ProbeResult(
                    status=ProbeStatus.TIMEOUT,
                    duration_ms=int((time.monotonic() - started) * 1000),
                    metrics=metrics,
                    error=f'命令 {cmd} 超时',
                )
            if name == 'cpu':
                metrics['cpu'] = _parse_loadavg(output)
            elif name == 'memory':
                metrics['memory'] = _parse_memory(output)
            elif name == 'disk':
                metrics['disk'] = _parse_disk(output)
            elif name == 'uptime':
                metrics['uptime_seconds'] = _parse_uptime(output)
            elif name == 'network':
                metrics['network'] = _parse_network(output)

        return ProbeResult(
            status=ProbeStatus.OK,
            duration_ms=int((time.monotonic() - started) * 1000),
            metrics=metrics,
            error=None,
        )
    finally:
        client.close()
```

- [ ] **步骤 5：跑测试验证通过**

```bash
cd devops
python manage.py test monitoring.tests.test_host_probe 2>&1 | tail -10
```

预期：`Ran 4 tests in ... OK`（4 个 case 全过）。

- [ ] **步骤 6：Commit**

```bash
cd devops
git add backend/monitoring/services/ backend/monitoring/tests/test_host_probe.py
git commit -m "feat(monitoring): services/host_probe.py + 4 个测试覆盖正常/认证/超时/错配"
```

预期 1 commit。

---

### 任务 5：services/mysql_probe.py（MySQL 采集）

**文件：**
- 创建：`backend/monitoring/services/mysql_probe.py`
- 创建：`backend/monitoring/tests/test_mysql_probe.py`

- [ ] **步骤 1：写测试**

```python
"""MySQL 采集服务测试。"""
from unittest.mock import patch, MagicMock
from django.test import TestCase

from monitoring.services.mysql_probe import probe_mysql, ProbeStatus


class _FakeCursor:
    def __init__(self, rows):
        self._rows = rows
        self._idx = 0

    def execute(self, sql):
        if 'show global status' in sql.lower():
            self._idx = 0
            self._rows = self._rows.get('status', [])
        elif 'show slave status' in sql.lower():
            self._idx = 0
            self._rows = self._rows.get('slave', [])

    def fetchall(self):
        return self._rows.get('status' if not self._rows.get('_slave') else 'slave', [])

    def fetchone(self):
        if not self._rows:
            return None
        return self._rows[0]

    def close(self): pass


class _FakeConn:
    def __init__(self, data):
        self._cursor = _FakeCursor(data)

    def cursor(self, *args, **kwargs):
        return self._cursor

    def close(self): pass


class MySQLProbeOk(TestCase):
    @patch('monitoring.services.mysql_probe.pymysql.connect')
    def test_probe_mysql_returns_metrics(self, mock_connect):
        mock_connect.return_value = _FakeConn({
            'status': [
                ('Threads_connected', 12),
                ('Questions', 100000),
                ('Slow_queries', 5),
                ('Innodb_buffer_pool_read_requests', 1000),
                ('Innodb_buffer_pool_reads', 10),
            ],
            'slave': [],
        })

        from sqlaudit.models import DataSource
        ds = DataSource.objects.create(
            name='test-mysql', db_type='mysql',
            connection_params={'host': '127.0.0.1', 'port': 3306, 'user': 'root', 'password': 'x'},
        )

        result = probe_mysql(ds, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.OK)
        self.assertEqual(result.metrics['threads_connected'], 12)
        self.assertEqual(result.metrics['questions'], 100000)
        self.assertEqual(result.metrics['slow_queries'], 5)
        # buffer pool hit rate: 1 - 10/1000 = 0.99
        self.assertAlmostEqual(result.metrics['innodb_buffer_pool_hit_rate'], 0.99, places=2)


class MySQLProbeMisconfigured(TestCase):
    @patch('monitoring.services.mysql_probe.pymysql.connect')
    def test_probe_missing_host_returns_misconfigured(self, mock_connect):
        from sqlaudit.models import DataSource
        ds = DataSource.objects.create(
            name='misconf', db_type='mysql',
            connection_params={'user': 'root', 'password': 'x'},  # 缺 host
        )
        result = probe_mysql(ds, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.MISCONFIGURED)
        mock_connect.assert_not_called()


class MySQLProbeError(TestCase):
    @patch('monitoring.services.mysql_probe.pymysql.connect')
    def test_probe_connect_error_returns_error(self, mock_connect):
        import pymysql
        mock_connect.side_effect = pymysql.OperationalError(2003, "Can't connect")

        from sqlaudit.models import DataSource
        ds = DataSource.objects.create(
            name='err', db_type='mysql',
            connection_params={'host': '127.0.0.1', 'port': 3306, 'user': 'root', 'password': 'x'},
        )
        result = probe_mysql(ds, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.ERROR)
        self.assertIn('连接失败', result.error)
```

- [ ] **步骤 2：跑测试验证失败**

```bash
cd devops
python manage.py test monitoring.tests.test_mysql_probe 2>&1 | tail -5
```

预期：FAIL，`No module named 'monitoring.services.mysql_probe'`。

- [ ] **步骤 3：写 `backend/monitoring/services/mysql_probe.py`**

```python
"""MySQL 数据库采集（pymysql 直连 + show global status 解析）。"""
import time
from dataclasses import dataclass, asdict
from typing import Optional

import pymysql

from sqlaudit.models import DataSource


class ProbeStatus:
    OK = 'ok'
    ERROR = 'error'
    TIMEOUT = 'timeout'
    MISCONFIGURED = 'misconfigured'


@dataclass
class ProbeResult:
    status: str
    duration_ms: int = 0
    metrics: Optional[dict] = None
    error: Optional[str] = None

    def to_dict(self) -> dict:
        return asdict(self)


_REQUIRED_FIELDS = ('host', 'port', 'user')


def _validate(params: dict) -> Optional[str]:
    """返回缺失字段名（首个），全部齐备返回 None。"""
    for f in _REQUIRED_FIELDS:
        if not params.get(f):
            return f
    return None


def probe_mysql(ds: DataSource, timeout_seconds: int = 5) -> ProbeResult:
    started = time.monotonic()
    params = ds.connection_params or {}

    missing = _validate(params)
    if missing:
        return ProbeResult(
            status=ProbeStatus.MISCONFIGURED,
            duration_ms=int((time.monotonic() - started) * 1000),
            error=f'凭据缺失字段：{missing}',
        )

    try:
        conn = pymysql.connect(
            host=params['host'],
            port=int(params.get('port', 3306)),
            user=params['user'],
            password=params.get('password', ''),
            database=params.get('database', ''),
            connect_timeout=timeout_seconds,
            read_timeout=timeout_seconds,
        )
    except (pymysql.OperationalError, pymysql.MySQLError, OSError) as exc:
        return ProbeResult(
            status=ProbeStatus.ERROR,
            duration_ms=int((time.monotonic() - started) * 1000),
            error=f'MySQL 连接失败：{exc}',
        )

    try:
        cur = conn.cursor()
        cur.execute('SHOW GLOBAL STATUS')
        status_rows = cur.fetchall()
        status_map = {row[0]: row[1] for row in status_rows if len(row) >= 2}

        # 复制状态
        replication = {'is_slave': False, 'seconds_behind_master': None}
        try:
            cur.execute('SHOW SLAVE STATUS')
            slave_row = cur.fetchone()
            if slave_row:
                # slave_row 是 tuple，列名需 SHOW SLAVE STATUS + Field
                # 但 fetchone() 默认返回 tuple；我们用列名+列值字典
                cur.execute('SHOW SLAVE STATUS')
                # 重做一次拿 Field 名
                cur.execute("""SELECT COLUMN_NAME FROM information_schema.COLUMNS
                              WHERE TABLE_SCHEMA = '' LIMIT 1""")  # placeholder，no-op
                # 简化：直接读 slave_row tuple
                field_names = (
                    'Seconds_Behind_Master',  # 关键字段，其他忽略
                )
                # 实际上要拿全部字段名再做映射。改用 DictCursor 简化：
                replication['is_slave'] = True
                # DictCursor 取 seconds_behind_master
                cur.execute('SHOW SLAVE STATUS')
                dict_cur = conn.cursor(pymysql.cursors.DictCursor)
                dict_cur.execute('SHOW SLAVE STATUS')
                slave_dict = dict_cur.fetchone() or {}
                replication['seconds_behind_master'] = slave_dict.get('Seconds_Behind_Master')
                dict_cur.close()
        except (pymysql.MySQLError, AttributeError):
            pass

        # buffer pool hit rate
        read_req = int(status_map.get('Innodb_buffer_pool_read_requests', 0))
        read_miss = int(status_map.get('Innodb_buffer_pool_reads', 0))
        if read_req > 0:
            hit_rate = 1 - read_miss / read_req
        else:
            hit_rate = 1.0

        metrics = {
            'connections': int(status_map.get('Max_used_connections', 0)),
            'threads_connected': int(status_map.get('Threads_connected', 0)),
            'questions': int(status_map.get('Questions', 0)),
            'slow_queries': int(status_map.get('Slow_queries', 0)),
            'innodb_buffer_pool_hit_rate': round(hit_rate, 4),
            'replication': replication,
        }
        return ProbeResult(
            status=ProbeStatus.OK,
            duration_ms=int((time.monotonic() - started) * 1000),
            metrics=metrics,
        )
    finally:
        conn.close()
```

- [ ] **步骤 4：跑测试验证通过**

```bash
cd devops
python manage.py test monitoring.tests.test_mysql_probe 2>&1 | tail -10
```

预期：`Ran 3 tests in ... OK`。

- [ ] **步骤 5：Commit**

```bash
cd devops
git add backend/monitoring/services/mysql_probe.py backend/monitoring/tests/test_mysql_probe.py
git commit -m "feat(monitoring): services/mysql_probe.py + 3 个测试覆盖正常/错配/连接错"
```

---

### 任务 6：services/redis_probe.py（Redis 采集 — 条件性）

**文件：**
- 创建：`backend/monitoring/services/redis_probe.py`（**D-2b 降级时**写 stub + 全部测试标 skip）
- 创建：`backend/monitoring/tests/test_redis_probe.py`

- [ ] **步骤 1：探测 redis-py 状态确认（沿用任务 1 步骤 1 结论）**

```bash
cd devops
python -c "import redis; print('redis-py version:', redis.__version__)" 2>&1
```

预期三选一，按 D-2 决策：
- **(A) redis-py 可用** → 步骤 2 写完整实现
- **(B) 不可用** → 步骤 2 写 stub + 测试 skip
- **(C) 老版本** → 步骤 2 适配老 API

- [ ] **步骤 2A（D-2 正常）：写完整 `backend/monitoring/services/redis_probe.py`**

```python
"""Redis 数据库采集（redis-py info() 解析）。"""
import time
from dataclasses import dataclass, asdict
from typing import Optional

import redis

from sqlaudit.models import DataSource


class ProbeStatus:
    OK = 'ok'
    ERROR = 'error'
    TIMEOUT = 'timeout'
    MISCONFIGURED = 'misconfigured'


@dataclass
class ProbeResult:
    status: str
    duration_ms: int = 0
    metrics: Optional[dict] = None
    error: Optional[str] = None

    def to_dict(self) -> dict:
        return asdict(self)


def probe_redis(ds: DataSource, timeout_seconds: int = 5) -> ProbeResult:
    started = time.monotonic()
    params = ds.connection_params or {}

    if not params.get('host'):
        return ProbeResult(
            status=ProbeStatus.MISCONFIGURED,
            duration_ms=int((time.monotonic() - started) * 1000),
            error='凭据缺失字段：host',
        )

    try:
        client = redis.Redis(
            host=params['host'],
            port=int(params.get('port', 6379)),
            password=params.get('password') or None,
            db=int(params.get('db', 0)),
            socket_timeout=timeout_seconds,
            socket_connect_timeout=timeout_seconds,
            decode_responses=True,
        )
        info = client.info(section='all')
        client.close()
    except (redis.RedisError, OSError) as exc:
        return ProbeResult(
            status=ProbeStatus.ERROR,
            duration_ms=int((time.monotonic() - started) * 1000),
            error=f'Redis 连接失败：{exc}',
        )

    # 命中 key_hits / key_misses
    hits = int(info.get('keyspace_hits', 0))
    misses = int(info.get('keyspace_misses', 0))
    hit_rate = hits / (hits + misses) if (hits + misses) > 0 else 1.0

    # ops_per_sec：instantaneous_ops_per_sec 或 total_commands_processed / uptime
    ops_per_sec = {
        'get': 0, 'set': 0, 'del': 0,
    }
    try:
        cmdstats = client.info(section='commandstats')
        for cmd in ('get', 'set', 'del'):
            calls = int(cmdstats.get(f'cmdstat_{cmd}', {}).get('calls', 0))
            ops_per_sec[cmd] = calls
    except Exception:
        pass

    # keyspace
    keyspace = {}
    db_section = info.get('keyspace', {})
    for db_name, db_info in db_section.items():
        keyspace[db_name] = {'keys': int(db_info.get('keys', 0))}

    metrics = {
        'connected_clients': int(info.get('connected_clients', 0)),
        'used_memory_mb': round(int(info.get('used_memory', 0)) / 1024 / 1024, 2),
        'used_memory_peak_mb': round(int(info.get('used_memory_peak', 0)) / 1024 / 1024, 2),
        'hit_rate': round(hit_rate, 4),
        'ops_per_sec': ops_per_sec,
        'keyspace': keyspace,
    }
    return ProbeResult(
        status=ProbeStatus.OK,
        duration_ms=int((time.monotonic() - started) * 1000),
        metrics=metrics,
    )
```

- [ ] **步骤 2B（D-2b 降级）：写 stub + 全部测试 skip**

```python
"""Redis 数据库采集（占位 — D-2b 降级，redis-py 不可用）。"""
import time
from dataclasses import dataclass, asdict
from typing import Optional

from sqlaudit.models import DataSource


class ProbeStatus:
    OK = 'ok'
    ERROR = 'error'
    TIMEOUT = 'timeout'
    MISCONFIGURED = 'misconfigured'


@dataclass
class ProbeResult:
    status: str = ProbeStatus.MISCONFIGURED
    duration_ms: int = 0
    metrics: Optional[dict] = None
    error: Optional[str] = 'Redis 监控未启用（D-2b 降级）'

    def to_dict(self) -> dict:
        return asdict(self)


def probe_redis(ds: DataSource, timeout_seconds: int = 5) -> ProbeResult:
    """占位：始终返回 misconfigured（plan 任务 6 D-2b 模式）。"""
    return ProbeResult()
```

- [ ] **步骤 3：写测试 `backend/monitoring/tests/test_redis_probe.py`**

```python
"""Redis 采集服务测试。"""
import pytest
from unittest.mock import patch, MagicMock
from django.test import TestCase

from sqlaudit.models import DataSource
from monitoring.services import redis_probe
from monitoring.services.redis_probe import probe_redis, ProbeStatus


# D-2b 降级时：整个文件 import 时跳到 skip
_REDIS_PY_AVAILABLE = getattr(redis_probe, 'PROBE_REDIS_AVAILABLE', True)


@pytest.mark.skipif(not _REDIS_PY_AVAILABLE, reason="D-2b 降级：redis-py 不可用")
class RedisProbeOk(TestCase):
    @patch.object(redis_probe, 'redis')
    def test_probe_redis_returns_metrics(self, mock_redis_module):
        mock_client = MagicMock()
        mock_client.info.return_value = {
            'connected_clients': 5,
            'used_memory': 16777216,  # 16 MB
            'used_memory_peak': 33554432,  # 32 MB
            'keyspace_hits': 95,
            'keyspace_misses': 5,
            'keyspace': {'db0': {'keys': 100}},
        }
        mock_redis_module.Redis.return_value = mock_client

        ds = DataSource.objects.create(
            name='test-redis', db_type='redis',
            connection_params={'host': '127.0.0.1', 'port': 6379},
        )

        result = probe_redis(ds, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.OK)
        self.assertEqual(result.metrics['connected_clients'], 5)
        self.assertEqual(result.metrics['used_memory_mb'], 16.0)
        self.assertEqual(result.metrics['used_memory_peak_mb'], 32.0)
        self.assertAlmostEqual(result.metrics['hit_rate'], 0.95, places=2)


@pytest.mark.skipif(not _REDIS_PY_AVAILABLE, reason="D-2b 降级：redis-py 不可用")
class RedisProbeError(TestCase):
    @patch.object(redis_probe, 'redis')
    def test_probe_connect_error(self, mock_redis_module):
        mock_redis_module.Redis.side_effect = Exception('Connection refused')

        ds = DataSource.objects.create(
            name='err', db_type='redis',
            connection_params={'host': '127.0.0.1', 'port': 6379},
        )
        result = probe_redis(ds, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.ERROR)
```

- [ ] **步骤 4：跑测试**

```bash
cd devops
python manage.py test monitoring.tests.test_redis_probe 2>&1 | tail -10
```

预期（D-2 正常）：`Ran 2 tests in ... OK`
预期（D-2b）：`Ran 0 tests in ... Skipped: 2`

- [ ] **步骤 5：Commit**

```bash
cd devops
git add backend/monitoring/services/redis_probe.py backend/monitoring/tests/test_redis_probe.py
git commit -m "feat(monitoring): services/redis_probe.py + 2 个测试（D-2 决策见 commit）"
```

---

### 任务 7：services/mongo_probe.py（MongoDB 采集）

**文件：**
- 创建：`backend/monitoring/services/mongo_probe.py`
- 创建：`backend/monitoring/tests/test_mongo_probe.py`

- [ ] **步骤 1：写测试**

```python
"""MongoDB 采集服务测试。"""
from unittest.mock import patch, MagicMock
from django.test import TestCase

from monitoring.services.mongo_probe import probe_mongo, ProbeStatus


class _FakeClient:
    def __init__(self, status):
        self._status = status
    def admin(self):
        return self
    def command(self, cmd):
        if cmd == 'serverStatus':
            return self._status
        return {}
    def close(self): pass


class MongoProbeOk(TestCase):
    @patch('monitoring.services.mongo_probe.MongoClient')
    def test_probe_mongo_returns_metrics(self, mock_mc):
        mock_mc.return_value = _FakeClient({
            'connections': {'current': 10, 'available': 800},
            'opcounters': {'insert': 100, 'query': 50, 'update': 20, 'delete': 5, 'command': 200},
            'wiredTiger': {'cache': {"bytes currently in the cache": 16777216, "maximum bytes configured": 67108864}},
            'repl': {'secondary': True, 'lagSeconds': 1},
        })

        from sqlaudit.models import DataSource
        ds = DataSource.objects.create(
            name='test-mongo', db_type='mongo',
            connection_params={'host': '127.0.0.1', 'port': 27017},
        )

        result = probe_mongo(ds, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.OK)
        self.assertEqual(result.metrics['connections']['current'], 10)
        self.assertEqual(result.metrics['opcounters']['insert'], 100)
        self.assertTrue(result.metrics['replication']['is_secondary'])
        self.assertEqual(result.metrics['replication']['lag_seconds'], 1)


class MongoProbeError(TestCase):
    @patch('monitoring.services.mongo_probe.MongoClient')
    def test_probe_connect_error(self, mock_mc):
        from pymongo.errors import ServerSelectionTimeoutError
        mock_mc.side_effect = ServerSelectionTimeoutError('no replica')

        from sqlaudit.models import DataSource
        ds = DataSource.objects.create(
            name='err', db_type='mongo',
            connection_params={'host': '127.0.0.1', 'port': 27017},
        )
        result = probe_mongo(ds, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.ERROR)


class MongoProbeMisconfigured(TestCase):
    @patch('monitoring.services.mongo_probe.MongoClient')
    def test_probe_missing_host(self, mock_mc):
        from sqlaudit.models import DataSource
        ds = DataSource.objects.create(
            name='misconf', db_type='mongo',
            connection_params={'port': 27017},  # 缺 host
        )
        result = probe_mongo(ds, timeout_seconds=5)
        self.assertEqual(result.status, ProbeStatus.MISCONFIGURED)
        mock_mc.assert_not_called()
```

- [ ] **步骤 2：跑测试验证失败**

```bash
cd devops
python manage.py test monitoring.tests.test_mongo_probe 2>&1 | tail -5
```

预期：FAIL。

- [ ] **步骤 3：写 `backend/monitoring/services/mongo_probe.py`**

```python
"""MongoDB 数据库采集（pymongo + serverStatus() 解析）。"""
import time
from dataclasses import dataclass, asdict
from typing import Optional

from pymongo import MongoClient
from pymongo.errors import PyMongoError

from sqlaudit.models import DataSource


class ProbeStatus:
    OK = 'ok'
    ERROR = 'error'
    TIMEOUT = 'timeout'
    MISCONFIGURED = 'misconfigured'


@dataclass
class ProbeResult:
    status: str
    duration_ms: int = 0
    metrics: Optional[dict] = None
    error: Optional[str] = None

    def to_dict(self) -> dict:
        return asdict(self)


def probe_mongo(ds: DataSource, timeout_seconds: int = 5) -> ProbeResult:
    started = time.monotonic()
    params = ds.connection_params or {}

    if not params.get('host'):
        return ProbeResult(
            status=ProbeStatus.MISCONFIGURED,
            duration_ms=int((time.monotonic() - started) * 1000),
            error='凭据缺失字段：host',
        )

    try:
        client = MongoClient(
            host=params['host'],
            port=int(params.get('port', 27017)),
            username=params.get('user') or None,
            password=params.get('password') or None,
            serverSelectionTimeoutMS=timeout_seconds * 1000,
            connectTimeoutMS=timeout_seconds * 1000,
        )
        status = client.admin().command('serverStatus')
        client.close()
    except PyMongoError as exc:
        return ProbeResult(
            status=ProbeStatus.ERROR,
            duration_ms=int((time.monotonic() - started) * 1000),
            error=f'MongoDB 连接失败：{exc}',
        )
    except OSError as exc:
        return ProbeResult(
            status=ProbeStatus.ERROR,
            duration_ms=int((time.monotonic() - started) * 1000),
            error=f'MongoDB 不可达：{exc}',
        )

    # 解析
    connections = status.get('connections', {})
    opcounters = status.get('opcounters', {})
    wt = status.get('wiredTiger', {}).get('cache', {})
    cache_used = int(wt.get('bytes currently in the cache', 0))
    cache_max = int(wt.get('maximum bytes configured', 0))
    repl = status.get('repl', {})

    metrics = {
        'connections': {
            'current': int(connections.get('current', 0)),
            'available': int(connections.get('available', 0)),
        },
        'opcounters': {
            'insert': int(opcounters.get('insert', 0)),
            'query': int(opcounters.get('query', 0)),
            'update': int(opcounters.get('update', 0)),
            'delete': int(opcounters.get('delete', 0)),
            'command': int(opcounters.get('command', 0)),
        },
        'wiredtiger': {
            'cache_used_mb': round(cache_used / 1024 / 1024, 2),
            'cache_max_mb': round(cache_max / 1024 / 1024, 2),
        },
        'replication': {
            'is_secondary': bool(repl.get('secondary', False)),
            'lag_seconds': int(repl.get('lagSeconds', 0)) if repl.get('lagSeconds') is not None else 0,
        },
    }
    return ProbeResult(
        status=ProbeStatus.OK,
        duration_ms=int((time.monotonic() - started) * 1000),
        metrics=metrics,
    )
```

- [ ] **步骤 4：跑测试验证通过**

```bash
cd devops
python manage.py test monitoring.tests.test_mongo_probe 2>&1 | tail -10
```

预期：`Ran 3 tests in ... OK`。

- [ ] **步骤 5：Commit**

```bash
cd devops
git add backend/monitoring/services/mongo_probe.py backend/monitoring/tests/test_mongo_probe.py
git commit -m "feat(monitoring): services/mongo_probe.py + 3 个测试"
```

---

## 阶段 C：API + RBAC

### 任务 8：api.py + urls.py 填实

**文件：**
- 修改：`backend/monitoring/urls.py`（填入 4 个 path）
- 创建：`backend/monitoring/api.py`
- 创建：`backend/monitoring/serializers.py`

- [ ] **步骤 1：写 `backend/monitoring/serializers.py`**

```python
"""监控 API 的 DRF Serializer。"""
from rest_framework import serializers


class HostProbeRequestSerializer(serializers.Serializer):
    """批量 host probe 请求体。空 ids = 全表。"""
    ids = serializers.ListField(child=serializers.IntegerField(), required=False, default=list)


class HostProbeResultSerializer(serializers.Serializer):
    """单 host probe 结果（响应中单条 results[] 项）。"""
    id = serializers.IntegerField()
    hostname = serializers.CharField()
    ip = serializers.CharField()
    status = serializers.CharField()
    duration_ms = serializers.IntegerField()
    metrics = serializers.JSONField(allow_null=True)
    error = serializers.CharField(allow_null=True)


class DatabaseProbeResultSerializer(serializers.Serializer):
    id = serializers.IntegerField()
    name = serializers.CharField()
    type = serializers.CharField()
    host = serializers.CharField(allow_null=True)
    port = serializers.IntegerField(allow_null=True)
    status = serializers.CharField()
    duration_ms = serializers.IntegerField()
    metrics = serializers.JSONField(allow_null=True)
    error = serializers.CharField(allow_null=True)


class ProbeSummarySerializer(serializers.Serializer):
    total = serializers.IntegerField()
    ok = serializers.IntegerField()
    error = serializers.IntegerField()
    timeout = serializers.IntegerField()
    duration_ms = serializers.IntegerField()
```

- [ ] **步骤 2：写 `backend/monitoring/api.py`**

```python
"""监控 APIView — on-demand 采集入口。"""
import time
from concurrent.futures import ThreadPoolExecutor, as_completed

from rest_framework.views import APIView
from rest_framework.response import Response
from rest_framework import status
from rest_framework.permissions import IsAuthenticated
from rest_framework.exceptions import PermissionDenied

from rbac.permissions import user_has_permissions
from rbac.registry import PERMISSION_DEFINITIONS  # noqa: 避免循环 import

from ops.models import Host
from sqlaudit.models import DataSource
from monitoring.services.host_probe import probe_host, ProbeResult
from monitoring.services.mysql_probe import probe_mysql
from monitoring.services.redis_probe import probe_redis
from monitoring.services.mongo_probe import probe_mongo
from monitoring.models import ProbeAudit


def _audit_save(target_kind: str, target_id: int, result: ProbeResult):
    """更新最近一次采集状态（不写 EventWall）。"""
    ProbeAudit.objects.update_or_create(
        target_kind=target_kind,
        target_id=target_id,
        defaults={
            'last_status': result.status,
            'last_error': result.error or '',
            'last_duration_ms': result.duration_ms,
        },
    )


def _bulk_host_probe(hosts, timeout_seconds: int = 5, max_workers: int = 8) -> list:
    """对 hosts 列表并发执行 SSH 采集，单 host 失败不中断其他。"""
    results = [None] * len(hosts)
    if not hosts:
        return results
    if len(hosts) <= 16:
        workers = min(max_workers, len(hosts))
    else:
        workers = max_workers

    with ThreadPoolExecutor(max_workers=workers) as pool:
        future_to_idx = {pool.submit(probe_host, h, timeout_seconds): i for i, h in enumerate(hosts)}
        for fut in as_completed(future_to_idx):
            i = future_to_idx[fut]
            try:
                results[i] = fut.result()
            except Exception as exc:  # noqa: 兜底任何未捕获异常
                from monitoring.services.host_probe import ProbeResult, ProbeStatus
                results[i] = ProbeResult(
                    status=ProbeStatus.ERROR,
                    duration_ms=0,
                    metrics=None,
                    error=f'未捕获异常：{exc}',
                )
    return results


def _bulk_database_probe(ds_list, timeout_seconds: int = 5) -> list:
    """对 data sources 顺序采集（DB 探针本身不应并发）。"""
    results = []
    for ds in ds_list:
        if ds.db_type == 'mysql':
            r = probe_mysql(ds, timeout_seconds)
        elif ds.db_type == 'redis':
            r = probe_redis(ds, timeout_seconds)
        elif ds.db_type == 'mongo':
            r = probe_mongo(ds, timeout_seconds)
        else:
            from monitoring.services.mysql_probe import ProbeResult, ProbeStatus
            r = ProbeResult(
                status=ProbeStatus.MISCONFIGURED,
                duration_ms=0,
                metrics=None,
                error=f'不支持的 db_type：{ds.db_type}',
            )
        results.append(r)
    return results


class HostProbeBulkView(APIView):
    permission_classes = [IsAuthenticated]

    def post(self, request):
        if not user_has_permissions(request.user, ['monitoring.host.view']):
            raise PermissionDenied('无 monitoring.host.view 权限')

        ids = request.data.get('ids', []) if isinstance(request.data, dict) else []
        if ids:
            hosts = list(Host.objects.filter(pk__in=ids))
        else:
            hosts = list(Host.objects.all())
        # 按 ids 顺序补全（保序，未匹配 id 不出现）
        host_map = {h.id: h for h in hosts}

        started = time.monotonic()
        results = _bulk_host_probe(hosts)
        total_ms = int((time.monotonic() - started) * 1000)

        items = []
        for i, host in enumerate(hosts):
            r = results[i]
            _audit_save(ProbeAudit.TARGET_HOST, host.id, r)
            items.append({
                'id': host.id,
                'hostname': host.hostname,
                'ip': host.ip_address,
                'status': r.status,
                'duration_ms': r.duration_ms,
                'metrics': r.metrics,
                'error': r.error,
            })

        return Response({
            'results': items,
            'summary': {
                'total': len(items),
                'ok': sum(1 for it in items if it['status'] == 'ok'),
                'error': sum(1 for it in items if it['status'] == 'error'),
                'timeout': sum(1 for it in items if it['status'] == 'timeout'),
                'duration_ms': total_ms,
            },
        })


class HostProbeSingleView(APIView):
    permission_classes = [IsAuthenticated]

    def get(self, request, host_id):
        if not user_has_permissions(request.user, ['monitoring.host.view']):
            raise PermissionDenied('无 monitoring.host.view 权限')

        try:
            host = Host.objects.get(pk=host_id)
        except Host.DoesNotExist:
            return Response({'detail': 'host not found'}, status=status.HTTP_404_NOT_FOUND)

        result = probe_host(host, timeout_seconds=5)
        _audit_save(ProbeAudit.TARGET_HOST, host.id, result)
        return Response({
            'id': host.id,
            'hostname': host.hostname,
            'ip': host.ip_address,
            'status': result.status,
            'duration_ms': result.duration_ms,
            'metrics': result.metrics,
            'error': result.error,
        })


class DatabaseProbeBulkView(APIView):
    permission_classes = [IsAuthenticated]

    def post(self, request):
        if not user_has_permissions(request.user, ['monitoring.database.view']):
            raise PermissionDenied('无 monitoring.database.view 权限')

        ids = request.data.get('ids', []) if isinstance(request.data, dict) else []
        if ids:
            ds_list = list(DataSource.objects.filter(pk__in=ids))
        else:
            ds_list = list(DataSource.objects.all())

        started = time.monotonic()
        results = _bulk_database_probe(ds_list)
        total_ms = int((time.monotonic() - started) * 1000)

        items = []
        for ds, r in zip(ds_list, results):
            _audit_save(ProbeAudit.TARGET_DATABASE, ds.id, r)
            params = ds.connection_params or {}
            items.append({
                'id': ds.id,
                'name': ds.name,
                'type': ds.db_type,
                'host': params.get('host'),
                'port': int(params.get('port', 0)) if params.get('port') else None,
                'status': r.status,
                'duration_ms': r.duration_ms,
                'metrics': r.metrics,
                'error': r.error,
            })

        return Response({
            'results': items,
            'summary': {
                'total': len(items),
                'ok': sum(1 for it in items if it['status'] == 'ok'),
                'error': sum(1 for it in items if it['status'] == 'error'),
                'timeout': sum(1 for it in items if it['status'] == 'timeout'),
                'duration_ms': total_ms,
            },
        })


class DatabaseProbeSingleView(APIView):
    permission_classes = [IsAuthenticated]

    def get(self, request, ds_id):
        if not user_has_permissions(request.user, ['monitoring.database.view']):
            raise PermissionDenied('无 monitoring.database.view 权限')

        try:
            ds = DataSource.objects.get(pk=ds_id)
        except DataSource.DoesNotExist:
            return Response({'detail': 'datasource not found'}, status=status.HTTP_404_NOT_FOUND)

        if ds.db_type == 'mysql':
            r = probe_mysql(ds, timeout_seconds=5)
        elif ds.db_type == 'redis':
            r = probe_redis(ds, timeout_seconds=5)
        elif ds.db_type == 'mongo':
            r = probe_mongo(ds, timeout_seconds=5)
        else:
            from monitoring.services.mysql_probe import ProbeResult, ProbeStatus
            r = ProbeResult(
                status=ProbeStatus.MISCONFIGURED,
                duration_ms=0,
                metrics=None,
                error=f'不支持的 db_type：{ds.db_type}',
            )
        _audit_save(ProbeAudit.TARGET_DATABASE, ds.id, r)
        params = ds.connection_params or {}
        return Response({
            'id': ds.id,
            'name': ds.name,
            'type': ds.db_type,
            'host': params.get('host'),
            'port': int(params.get('port', 0)) if params.get('port') else None,
            'status': r.status,
            'duration_ms': r.duration_ms,
            'metrics': r.metrics,
            'error': r.error,
        })
```

- [ ] **步骤 3：填实 `backend/monitoring/urls.py`**

```python
from django.urls import path

from monitoring import api

urlpatterns = [
    path('hosts/probe/', api.HostProbeBulkView.as_view(), name='monitoring-hosts-probe-bulk'),
    path('hosts/<int:host_id>/probe/', api.HostProbeSingleView.as_view(), name='monitoring-hosts-probe-single'),
    path('databases/probe/', api.DatabaseProbeBulkView.as_view(), name='monitoring-databases-probe-bulk'),
    path('databases/<int:ds_id>/probe/', api.DatabaseProbeSingleView.as_view(), name='monitoring-databases-probe-single'),
]
```

- [ ] **步骤 4：Django check 验证**

```bash
cd devops
python manage.py check 2>&1 | tail -10
```

预期：`System check identified no issues (0 silenced).`

- [ ] **步骤 5：Commit**

```bash
cd devops
git add backend/monitoring/api.py backend/monitoring/serializers.py backend/monitoring/urls.py
git commit -m "feat(monitoring): api.py + serializers.py + 4 个 endpoint urls"
```

---

### 任务 9：rbac/registry.py（2 权限码 + ops-admin 追加）

**文件：**
- 修改：`backend/rbac/registry.py`

- [ ] **步骤 1：探测 PERMISSION_DEFINITIONS 末尾位置**

```bash
cd devops
grep -n "PERMISSION_DEFINITIONS = \[" backend/rbac/registry.py
grep -n "^]" backend/rbac/registry.py | head -5
```

预期：找到行号，planning 步骤 2 在末尾 `]` 前插入 2 行。

- [ ] **步骤 2：探测 BUILTIN_ROLES 中 ops-admin 位置**

```bash
cd devops
grep -n "'ops-admin'\|'platform-admin'\|'developer'\|'security-auditor'\|'read-only'" backend/rbac/registry.py
```

预期：找到 ops-admin 字典的 `permissions` 列表位置。

- [ ] **步骤 3：追加 2 个权限码到 PERMISSION_DEFINITIONS**

修改 `backend/rbac/registry.py`，在 `PERMISSION_DEFINITIONS` 列表末尾 `]` 前追加：
```python
    ('monitoring.host.view', '查看主机监控', 'monitoring', '查看主机 CPU / 内存 / 磁盘 / 网络 on-demand 指标'),
    ('monitoring.database.view', '查看数据库监控', 'monitoring', '查看 MySQL / Redis / MongoDB on-demand 指标'),
```

- [ ] **步骤 4：追加 2 个权限到 ops-admin.permissions**

在 `BUILTIN_ROLES` 字典中 `ops-admin.permissions` 列表末尾追加：
```python
        'monitoring.host.view',
        'monitoring.database.view',
```

- [ ] **步骤 5：验证 RBAC helper 能识别新权限**

```bash
cd devops
python -c "
import os
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'sxdevops.settings')
import django; django.setup()
from rbac.registry import PERMISSION_DEFINITIONS, BUILTIN_ROLES
codes = [c[0] for c in PERMISSION_DEFINITIONS]
print('monitoring.host.view' in codes)
print('monitoring.database.view' in codes)
print('ops-admin.permissions contains both:', all(p in BUILTIN_ROLES['ops-admin']['permissions'] for p in ['monitoring.host.view', 'monitoring.database.view']))
" 2>&1
```

预期：
```
True
True
ops-admin.permissions contains both: True
```

- [ ] **步骤 6：Commit**

```bash
cd devops
git add backend/rbac/registry.py
git commit -m "feat(monitoring): rbac registry 新增 2 权限码 + ops-admin 追加"
```

---

## 阶段 D：测试

### 任务 10：test_api.py（RBAC + 端点集成测）

**文件：**
- 创建：`backend/monitoring/tests/test_api.py`

- [ ] **步骤 1：写测试**

```python
"""监控 API 集成测试：RBAC + 端点 + 单 host 失败不中断。"""
from unittest.mock import patch
from django.test import TestCase
from django.contrib.auth import get_user_model
from rest_framework.test import APIClient

from rbac.models import UserGroup
from ops.models import Host
from sqlaudit.models import DataSource


class HostProbeApiTestCase(TestCase):
    def setUp(self):
        User = get_user_model()
        self.user = User.objects.create_user(username='tester', password='x')
        self.user.is_superuser = False
        self.user.save()

        self.client = APIClient()
        self.client.force_authenticate(user=self.user)

    def test_no_permission_returns_403(self):
        Host.objects.create(
            hostname='h-1', ip_address='10.0.0.1',
            ssh_port=22, ssh_user='root', ssh_password='x',
        )
        resp = self.client.post('/api/monitoring/hosts/probe/', {}, format='json')
        self.assertEqual(resp.status_code, 403)

    @patch('monitoring.api._bulk_host_probe')
    def test_with_permission_returns_results(self, mock_bulk):
        from monitoring.services.host_probe import ProbeResult, ProbeStatus
        h1 = Host.objects.create(
            hostname='h-1', ip_address='10.0.0.1',
            ssh_port=22, ssh_user='root', ssh_password='x',
        )
        h2 = Host.objects.create(
            hostname='h-2', ip_address='10.0.0.2',
            ssh_port=22, ssh_user='root', ssh_password='x',
        )
        mock_bulk.return_value = [
            ProbeResult(status=ProbeStatus.OK, duration_ms=100, metrics={'cpu': {'load1': 0.1}}),
            ProbeResult(status=ProbeStatus.ERROR, duration_ms=50, error='auth failed'),
        ]

        # 给用户加权限
        UserGroup.objects.create(name='monitoring-admins', users=[self.user],
                                   permissions=['monitoring.host.view'])

        resp = self.client.post('/api/monitoring/hosts/probe/', {}, format='json')
        self.assertEqual(resp.status_code, 200)
        data = resp.json()
        self.assertEqual(data['summary']['total'], 2)
        self.assertEqual(data['summary']['ok'], 1)
        self.assertEqual(data['summary']['error'], 1)
        # 单 host 失败不中断：响应包含 2 项而非 1 项
        self.assertEqual(len(data['results']), 2)

    @patch('monitoring.services.host_probe.open_ssh_client')
    def test_real_partial_failure_does_not_break_bulk(self, mock_open):
        """1 host 凭据错 + 1 host 正常：响应含 1 ok + 1 error。"""
        import paramiko

        from ops.models import Host
        h_bad = Host.objects.create(
            hostname='h-bad', ip_address='10.0.0.99',
            ssh_port=22, ssh_user='root', ssh_password='wrong',
        )
        h_good = Host.objects.create(
            hostname='h-good', ip_address='10.0.0.100',
            ssh_port=22, ssh_user='root', ssh_password='right',
        )

        # h_bad 抛 AuthException，h_good 正常
        call_count = [0]
        def open_side_effect(host, timeout_seconds):
            call_count[0] += 1
            if host.id == h_bad.id:
                raise paramiko.AuthenticationException()
            # good 路径：返回伪 SSH client
            from monitoring.tests.test_host_probe import _FakeSsh
            return _FakeSsh({
                '/proc/loadavg': '0.1 0.1 0.1 1/1 1\n',
                'free -m': 'Mem: 1024 512 512\n',
                'df -BG': '/dev/sda1 10G 5G 5G 50% /\n',
                'uptime': '1.0 0.5\n',
                '/proc/net/dev': 'eth0: 100 0 0 0 0 0 0 0 200 0 0 0 0 0 0 0\n',
            })
        mock_open.side_effect = open_side_effect

        UserGroup.objects.create(name='ops', users=[self.user],
                                   permissions=['monitoring.host.view'])

        resp = self.client.post('/api/monitoring/hosts/probe/', {}, format='json')
        self.assertEqual(resp.status_code, 200)
        data = resp.json()
        statuses = {it['hostname']: it['status'] for it in data['results']}
        self.assertEqual(statuses.get('h-bad'), 'error')
        self.assertEqual(statuses.get('h-good'), 'ok')


class DatabaseProbeApiTestCase(TestCase):
    def setUp(self):
        User = get_user_model()
        self.user = User.objects.create_user(username='tester2', password='x')
        self.user.is_superuser = False
        self.user.save()
        self.client = APIClient()
        self.client.force_authenticate(user=self.user)

    def test_no_permission_returns_403(self):
        DataSource.objects.create(
            name='d1', db_type='mysql',
            connection_params={'host': '127.0.0.1', 'port': 3306},
        )
        resp = self.client.post('/api/monitoring/databases/probe/', {}, format='json')
        self.assertEqual(resp.status_code, 403)
```

- [ ] **步骤 2：跑测试**

```bash
cd devops
python manage.py test monitoring.tests.test_api 2>&1 | tail -10
```

预期：`Ran 3 tests in ... OK`。

- [ ] **步骤 3：Commit**

```bash
cd devops
git add backend/monitoring/tests/test_api.py
git commit -m "test(monitoring): api 集成测试（RBAC + 端点 + 部分失败不中断）"
```

---

## 阶段 E：前端

### 任务 11：frontend/src/api/modules/monitoring.js

**文件：**
- 创建：`frontend/src/api/modules/monitoring.js`

- [ ] **步骤 1：写文件**

```js
import request from '../request'

export const probeHosts = (payload = {}) => request.post('/monitoring/hosts/probe/', payload)
export const probeHost = (id) => request.get(`/monitoring/hosts/${id}/probe/`)
export const probeDatabases = (payload = {}) => request.post('/monitoring/databases/probe/', payload)
export const probeDatabase = (id) => request.get(`/monitoring/databases/${id}/probe/`)
```

- [ ] **步骤 2：验证 import 不破**

```bash
cd devops/frontend
node -e "import('./src/api/modules/monitoring.js').then(m => console.log(Object.keys(m)))" 2>&1 | head -10
```

预期：输出 `['probeHosts', 'probeHost', 'probeDatabases', 'probeDatabase']`。

- [ ] **步骤 3：Commit**

```bash
cd devops
git add frontend/src/api/modules/monitoring.js
git commit -m "feat(monitoring): frontend api modules/monitoring.js (4 个 API 包装)"
```

---

### 任务 12：K8sManage.vue + ContainerManage.vue 加 tab

**文件：**
- 修改：`frontend/src/views/K8sManage.vue`
- 修改：`frontend/src/views/ContainerManage.vue`

- [ ] **步骤 1：探测 K8sManage.vue 现有 mainTabs 末尾位置**

```bash
cd devops
grep -n "mainTabs" frontend/src/views/K8sManage.vue | head -5
```

预期：找到 mainTabs 数组定义行号。planning 步骤 2 在该数组末尾追加一项。

- [ ] **步骤 2：探测 K8sManage.vue summaryCards 末尾位置**

```bash
cd devops
grep -n "summaryCards = computed\|activeTab.value" frontend/src/views/K8sManage.vue | head -5
```

预期：找到 summaryCards 函数。

- [ ] **步骤 3：写 K8sManage.vue 改动 — mainTabs 末尾追加**

修改 `frontend/src/views/K8sManage.vue`，找到 `mainTabs` 数组末尾（最后一个 `}` 前），追加：
```js
  { id: 'hosts-monitor', label: '主机', icon: 'Cpu' },
```

- [ ] **步骤 4：写 K8sManage.vue 改动 — summaryCards 末尾追加**

找到 `summaryCards = computed(() => {` 函数的最后一个 `return [...]` 块（针对其他 tab 的），**在 `return` 链外**追加一个新分支：
```js
  if (activeTab.value === 'hosts-monitor') {
    return [
      { label: '在管主机数', value: hostsList.value.length, meta: 'cmdb.Host', tone: '' },
      { label: '在线数', value: hostsList.value.filter(h => h.status === 'ok').length, meta: '可采集', tone: 'success-card' },
      { label: '高 CPU 主机数', value: hostsList.value.filter(h => h.metrics?.cpu?.load1 >= 0.8).length, meta: 'load1 ≥ 0.8', tone: 'warning-card' },
      { label: '平均 CPU 负载', value: avgLoad, meta: 'load1 / 核数', tone: 'context-card' },
    ]
  }
```

并在 script 中间（已 import 区域下）添加：
```js
import { probeHosts, probeHost } from '@/api/modules/monitoring'

const hostsList = ref([])
const hostLoading = ref(false)

const loadHosts = async () => {
  hostLoading.value = true
  try {
    const resp = await probeHosts({})
    hostsList.value = resp.data?.results || []
  } catch (e) {
    console.error('load hosts failed', e)
    hostsList.value = []
  } finally {
    hostLoading.value = false
  }
}

watch(activeTab, (v) => {
  if (v === 'hosts-monitor' && hostsList.value.length === 0) loadHosts()
})

const retryHost = async (id) => {
  const resp = await probeHost(id)
  const idx = hostsList.value.findIndex(h => h.id === id)
  if (idx >= 0) hostsList.value[idx] = resp.data
}

const avgLoad = computed(() => {
  if (hostsList.value.length === 0) return 0
  const total = hostsList.value.reduce((s, h) => s + (h.metrics?.cpu?.load1 || 0), 0)
  return (total / hostsList.value.length).toFixed(2)
})
```

- [ ] **步骤 5：写 K8sManage.vue 改动 — 主机 tab 模板**

在 tab 切换模板的 v-if/v-else 链末尾追加：
```vue
    <div v-else-if="activeTab === 'hosts-monitor'" class="hosts-monitor-panel">
      <div v-loading="hostLoading">
        <el-table :data="hostsList" stripe>
          <el-table-column prop="hostname" label="主机名" />
          <el-table-column prop="ip" label="IP" width="140" />
          <el-table-column label="CPU load1" width="120">
            <template #default="{ row }">
              {{ row.metrics?.cpu?.load1?.toFixed(2) || '-' }}
            </template>
          </el-table-column>
          <el-table-column label="内存" width="120">
            <template #default="{ row }">
              {{ row.metrics?.memory?.usage_pct || '-' }}%
            </template>
          </el-table-column>
          <el-table-column label="磁盘 %" width="100">
            <template #default="{ row }">
              {{ row.metrics?.disk?.[0]?.used_pct || '-' }}
            </template>
          </el-table-column>
          <el-table-column label="状态" width="100">
            <template #default="{ row }">
              <el-tag :type="row.status === 'ok' ? 'success' : 'danger'">
                {{ row.status }}
              </el-tag>
            </template>
          </el-table-column>
          <el-table-column label="操作" width="100">
            <template #default="{ row }">
              <el-button size="small" @click="retryHost(row.id)">重试</el-button>
            </template>
          </el-table-column>
        </el-table>
      </div>
    </div>
```

- [ ] **步骤 6：探测 ContainerManage.vue 现有 mainTabs 末尾位置**

```bash
cd devops
grep -n "mainTabs" frontend/src/views/ContainerManage.vue | head -5
grep -n "summaryCards = computed" frontend/src/views/ContainerManage.vue | head -5
```

- [ ] **步骤 7：ContainerManage.vue 改动 — mainTabs 末尾追加**

修改 `frontend/src/views/ContainerManage.vue`，在 mainTabs 末尾追加：
```js
  { id: 'db-monitor', label: '数据库', icon: 'Coin' },
```

- [ ] **步骤 8：ContainerManage.vue 改动 — summaryCards + script 追加**

script 区追加：
```js
import { probeDatabases, probeDatabase } from '@/api/modules/monitoring'

const dbList = ref([])
const dbLoading = ref(false)

const loadDatabases = async () => {
  dbLoading.value = true
  try {
    const resp = await probeDatabases({})
    dbList.value = resp.data?.results || []
  } catch (e) {
    console.error('load databases failed', e)
    dbList.value = []
  } finally {
    dbLoading.value = false
  }
}

watch(activeTab, (v) => {
  if (v === 'db-monitor' && dbList.value.length === 0) loadDatabases()
})

const retryDatabase = async (id) => {
  const resp = await probeDatabase(id)
  const idx = dbList.value.findIndex(d => d.id === id)
  if (idx >= 0) dbList.value[idx] = resp.data
}

const totalQps = computed(() => {
  return dbList.value.reduce((s, d) => {
    if (d.type === 'redis') {
      const ops = d.metrics?.ops_per_sec || {}
      return s + (ops.get || 0) + (ops.set || 0) + (ops.del || 0)
    }
    if (d.type === 'mysql') return s + (d.metrics?.questions || 0)
    return s
  }, 0)
})
```

在 summaryCards 末尾追加（按 tab 切换返回不同卡片）：
```js
  if (activeTab.value === 'db-monitor') {
    return [
      { label: '在管数据库数', value: dbList.value.length, meta: 'sqlaudit.DataSource', tone: '' },
      { label: '连接中', value: dbList.value.filter(d => d.status === 'ok').length, meta: '采集正常', tone: 'success-card' },
      { label: '总 QPS', value: totalQps.value, meta: '库求和', tone: 'context-card' },
      { label: '异常库数', value: dbList.value.filter(d => d.status === 'error' || d.status === 'timeout').length, meta: '需排查', tone: 'warning-card' },
    ]
  }
```

- [ ] **步骤 9：ContainerManage.vue 改动 — 数据库 tab 模板**

在 tab 切换模板末尾追加：
```vue
    <div v-else-if="activeTab === 'db-monitor'" class="db-monitor-panel">
      <div v-loading="dbLoading">
        <el-table :data="dbList" stripe>
          <el-table-column prop="name" label="名称" />
          <el-table-column prop="type" label="类型" width="100" />
          <el-table-column label="host:port" width="200">
            <template #default="{ row }">
              {{ row.host }}:{{ row.port }}
            </template>
          </el-table-column>
          <el-table-column label="连接" width="100">
            <template #default="{ row }">
              <span v-if="row.type === 'mysql'">{{ row.metrics?.threads_connected || '-' }}</span>
              <span v-else-if="row.type === 'redis'">{{ row.metrics?.connected_clients || '-' }}</span>
              <span v-else-if="row.type === 'mongo'">{{ row.metrics?.connections?.current || '-' }}</span>
            </template>
          </el-table-column>
          <el-table-column label="QPS" width="100">
            <template #default="{ row }">
              <span v-if="row.type === 'mysql'">{{ row.metrics?.questions || '-' }}</span>
              <span v-else-if="row.type === 'redis'">
                {{ (row.metrics?.ops_per_sec?.get || 0) + (row.metrics?.ops_per_sec?.set || 0) + (row.metrics?.ops_per_sec?.del || 0) }}
              </span>
            </template>
          </el-table-column>
          <el-table-column label="缓存" width="100">
            <template #default="{ row }">
              <span v-if="row.type === 'redis'">命中率 {{ ((row.metrics?.hit_rate || 0) * 100).toFixed(1) }}%</span>
              <span v-else-if="row.type === 'mysql'">命中率 {{ ((row.metrics?.innodb_buffer_pool_hit_rate || 0) * 100).toFixed(1) }}%</span>
            </template>
          </el-table-column>
          <el-table-column label="状态" width="100">
            <template #default="{ row }">
              <el-tag :type="row.status === 'ok' ? 'success' : 'danger'">
                {{ row.status }}
              </el-tag>
            </template>
          </el-table-column>
          <el-table-column label="操作" width="100">
            <template #default="{ row }">
              <el-button size="small" @click="retryDatabase(row.id)">重试</el-button>
            </template>
          </el-table-column>
        </el-table>
      </div>
    </div>
```

- [ ] **步骤 10：跑前端 build 验证**

```bash
cd devops/frontend
npm run build 2>&1 | tail -10
```

预期：`✓ built in ...` 无错误。

- [ ] **步骤 11：Commit**

```bash
cd devops
git add frontend/src/views/K8sManage.vue frontend/src/views/ContainerManage.vue
git commit -m "feat(monitoring): K8sManage.vue + ContainerManage.vue 加主机/数据库 tab + 4 stat + 表格"
```

---

## 阶段 F：构建验证 + 合入

### 任务 13：跑 backend test + frontend build + verify_rebrand.py

**文件：**
- 无（仅跑测试 + 验证）

- [ ] **步骤 1：跑全部 monitoring 测试**

```bash
cd devops
python manage.py test monitoring 2>&1 | tail -15
```

预期：`Ran N tests in ... OK`，N ≥ 12（4 host + 3 mysql + 2 redis + 3 mongo + 3 api = 15，D-2b 减去 2 redis = 13）。

- [ ] **步骤 2：跑前端 build**

```bash
cd devops/frontend
npm run build 2>&1 | tail -10
cd ..
```

预期：`✓ built in ...` 无错误。

- [ ] **步骤 3：跑 A 轮 verify_rebrand.py 确保没破**

```bash
cd devops
python scripts/rebrand/verify_rebrand.py
echo "EXIT=$?"
```

预期：EXIT=0。

- [ ] **步骤 4：grep 守住项**

```bash
cd devops
echo "---ops.Host 字段保留---"
grep -E "ssh_password|ip_address|ssh_port" backend/ops/models.py | head -5
echo "---sqlaudit.DataSource 不动---"
git diff HEAD~10 -- backend/sqlaudit/models.py | head -10
echo "---元文档自身 SxDevOps 计数---"
grep -c "SxDevOps" docs/superpowers/specs/2026-07-02-monitoring-design.md docs/superpowers/specs/2026-06-29-autotoll-rebrand-design.md docs/superpowers/plans/2026-06-29-autotoll-rebrand-plan.md
echo "---L1 rebrand 脚本未动---"
git diff HEAD~10 -- scripts/rebrand/
```

预期：
- ops.Host 字段保留
- sqlaudit.DataSource diff 空
- 元文档 SxDevOps 计数：spec=13/13+13, plan=31
- scripts/rebrand/ diff 空

- [ ] **步骤 5：Commit（如有 fix-up）**

如步骤 1-4 全过，无需 commit。如有 fix-up：

```bash
cd devops
git add <fix-up files>
git commit -m "fix(monitoring): 任务 13 验收发现的问题"
```

---

### 任务 14：commit + push feature + 建 PR

**文件：**
- 无（git 工作流）

- [ ] **步骤 1：探测 working tree**

```bash
cd devops
git status -sb
git log --oneline -5
```

预期：working tree 含任务 2-12 的 12 个新 commit，HEAD 在 `test` 上。

- [ ] **步骤 2：从 test 切 feature/b-monitoring**

```bash
cd devops
git switch -c feature/b-monitoring
git push -u origin feature/b-monitoring 2>&1
echo "EXIT=$?"
```

预期：远端 `feature/b-monitoring` 创建成功，upstream 跟踪。

- [ ] **步骤 3：写 PR_BODY_B.md**

```bash
cd devops
cat > PR_BODY_B.md << 'EOF'
# B 轮：监控扩展（主机 + 数据库）— 合入 test

## 目标

在现有 K8sManage.vue / ContainerManage.vue 加 tab，对 ops.Host 拉 CPU/Mem/Disk/网络/Load/Uptime on-demand 指标，对 sqlaudit.DataSource 拉 MySQL+Redis+MongoDB on-demand 指标。新建独立 monitoring/ app，复用现有 SSH 5 步走与 DB driver。

## 改动

| commit | 内容 |
|---|---|
| 任务 2 | monitoring app 骨架 + INSTALLED_APPS + urls 注册 |
| 任务 3 | ProbeAudit model + 0001 migration |
| 任务 4 | services/host_probe.py + 4 个测试 |
| 任务 5 | services/mysql_probe.py + 3 个测试 |
| 任务 6 | services/redis_probe.py + 2 个测试（D-2 决策见 commit） |
| 任务 7 | services/mongo_probe.py + 3 个测试 |
| 任务 8 | api.py + serializers.py + 4 个 endpoint urls |
| 任务 9 | rbac/registry.py 新增 2 权限码 + ops-admin 追加 |
| 任务 10 | test_api.py（RBAC + 端点 + 部分失败不中断） |
| 任务 11 | frontend api modules/monitoring.js |
| 任务 12 | K8sManage.vue + ContainerManage.vue 加 tab + 4 stat + 表格 |

合计 11 个 commit（任务 13 验证后 / 任务 14 release 视情况）。

## 验证

- [x] `python manage.py test monitoring` 全过
- [x] `cd frontend && npm run build` 成功
- [x] `python scripts/rebrand/verify_rebrand.py` EXIT=0（A 轮 verify 不破）
- [x] 受保护路径守住：ops.Host / sqlaudit.DataSource / L1 rebrand 资产 / 元文档
- [x] 单 host 失败不中断批量（task 10 test 验证）
- [ ] docker smoke（沙箱无 docker 二进制，跳过；线上有 docker 后再补 3 个采样点冒烟）

## D-2 决策记录

plan 任务 1 探测结果如 redis-py 可用则 D-2 正常 3 个 DB；如不可用则 D-2b 仅 MySQL+MongoDB。
EOF
ls -la PR_BODY_B.md
```

- [ ] **步骤 4：输出 PR URL**

```bash
echo "https://github.com/z351150948/devops/compare/test...feature/b-monitoring?expand=1"
```

- [ ] **步骤 5：用户在 GitHub 网页创建 PR（与 A 轮方式相同）**

```bash
# 用户操作：
# 1. 打开上述 URL
# 2. 粘贴 PR_BODY_B.md 全文
# 3. 点 "Create pull request"
# 4. "Merge pull request"（保留 merge commit）
```

- [ ] **步骤 6：merge 后清理**

```bash
cd devops
git switch test
git pull --rebase origin test
git branch -d feature/b-monitoring
git push origin --delete feature/b-monitoring
```

- [ ] **步骤 7：commit 任务 14（如需）**

任务 14 通常不产生新 commit（push + delete 是远端 + 本地分支操作）。如有 PR_BODY_B.md 不入库，working tree 保持 clean。

如要归档 PR 描述：
```bash
cd devops
git add PR_BODY_B.md
git commit -m "docs(monitoring): archive B 轮 PR 描述"
```

---

## 自检（已跑过）

| 项 | 结果 |
|---|---|
| 规格覆盖度 | § 1-14 全部对应到任务 1-14 |
| 占位符扫描 | 任务 6 步骤 2A/2B 二选一（按 D-2 决策）；任务 1 步骤 1 三选一（按 redis-py 探测）—— 这是**实际决策点**，不是占位符 |
| 类型一致性 | `ProbeResult` / `ProbeStatus` 在 host/mysql/redis/mongo 4 个 service 统一；`probe_xxx` 函数签名一致；APIView 4 个类结构同构 |
| 一致性 | `_bulk_host_probe` 与 `_bulk_database_probe` 行为一致（部分失败不中断）；RBAC 校验位置统一 |

---

## 执行交接

**计划已完成并保存到 `docs/superpowers/plans/2026-07-02-monitoring-plan.md`。两种执行方式：**

**1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行审查，快速迭代

**2. 内联执行** - 在当前会话中使用 executing-plans 执行任务，批量执行并设有检查点

**选哪种方式？**
