# SxDevOps 简历项目描述整理

本文档整理了 `SxDevOps 运维与交付平台` 在简历、招聘平台、项目汇报中的常用表达，方便直接复制使用。

---

## 1. 简历长版

- 负责设计并实现 `SxDevOps` 运维与交付平台，基于 `Django / DRF / Channels + Vue3 / Element Plus` 搭建统一运维门户，覆盖 `CMDB`、应用发布、容器运维、日志审计、Nginx 管理与 `IaC` 资源编排等能力。
- 主导落地企业级“应用发布”模块，支持 `Docker` 与 `K8s` 双发布模式、审批流、灰度 / 批次发布、回滚、重新执行，并实现发布结果自动同步 `CMDB`、建立应用与目标资源关系。
- 完成 `K8s` 集群运维增强，包括集群摘要、`Pod Terminal`、工作负载扩缩容、`ConfigMap/Secret` 差异预览与历史回滚；补齐 Docker 容器日志、镜像治理等能力，提升平台可观测与可运维性。
- 建立统一 `RBAC` 权限模型，覆盖页面、接口、按钮与 WebSocket 场景；同时完善演示数据、测试用例与 README 展示体系，支持对外演示与项目汇报。

---

## 2. 简历短版

- 基于 `Django + Vue3` 设计并实现企业级运维与交付平台，涵盖 `CMDB`、应用发布、容器运维、日志审计与 `IaC` 编排。
- 主导应用发布模块建设，支持 `Docker/K8s` 双模式、审批流、灰度 / 批次发布、回滚与 `CMDB` 自动关联。
- 完善 `K8s` 运维、Docker 治理与统一 `RBAC` 权限体系，形成资源治理到发布交付的完整闭环。

---

## 3. 四段式简历版

### 3.1 项目描述

- 参与设计并持续迭代 `SxDevOps` 运维与交付平台，基于 `Django / DRF / Channels` 与 `Vue 3 / Element Plus` 构建统一运维门户，覆盖 `CMDB`、应用发布、容器运维、日志审计、Nginx 管理、告警中心与 `IaC` 资源编排等核心场景。

### 3.2 技术亮点

- 实现企业级“应用发布”模块，支持 `Docker 环境` 与 `K8s 集群` 双模式发布，具备审批流、灰度 / 批次发布、回滚、重新执行、状态查看与 `CMDB` 自动关联能力。
- 完成 `K8s` 运维增强，包括集群摘要、`Pod Terminal`、工作负载扩缩容、`ConfigMap/Secret` 差异预览与历史回滚。
- 补齐 Docker 治理能力，支持容器日志查看、详情检查、镜像批量删除与悬空镜像清理。
- 建立统一 `RBAC` 权限模型，覆盖页面、接口、按钮与 WebSocket 场景。

### 3.3 个人贡献

- 主导应用发布模块的前后端设计与落地，完成发布单模型、审批流节点、发布策略、回滚重试、CMDB 联动等核心能力建设。
- 完成发布详情、列表筛选、目标展示、系统 / 环境对齐等产品化页面改造，并与 `CMDB`、`Docker`、`K8s` 模块打通。
- 编写发布链路相关测试、演示数据与文档，完善 README 展示内容，支持项目演示、汇报与交付说明。

### 3.4 难点与解决方案

- **多环境发布统一建模**：将 `Docker` 与 `K8s` 两类目标抽象到统一发布单模型中，同时兼容不同目标字段与状态查询逻辑。
- **发布闭环落地**：将审批、异步执行、回滚、重试、状态回写、CMDB 同步串成完整链路，避免只停留在“提单”层面。
- **真实场景兼容性**：针对 demo 环境和真实环境差异，补充降级与兜底逻辑，保证演示数据可用、详情页不报错。
- **权限一致性**：通过统一 `RBAC` 约束菜单、页面操作和接口访问，确保平台能力扩展时权限模型不失控。

---

## 4. BOSS / 拉勾 / 猎聘风格

### 4.1 项目名称

- `SxDevOps 运维与交付平台`

### 4.2 项目介绍

- 面向企业内部的统一运维与交付平台，覆盖 `CMDB`、应用发布、容器运维、日志审计、Nginx 管理、告警中心与 `IaC` 资源编排等场景，支持资源治理、应用发布、运维排障与交付编排闭环。

### 4.3 职责描述

- 负责应用发布模块的前后端设计与实现，支持 `Docker / K8s` 双模式发布、审批流、灰度 / 批次发布、回滚、重新执行与状态查看。
- 负责应用发布与 `CMDB` 的联动设计，发布后自动登记应用配置项、发布目标配置项，并建立资源关系。
- 参与 `K8s` 运维能力建设，落地集群摘要、`Pod Terminal`、工作负载扩缩容、配置版本回滚等功能。
- 参与 Docker 治理、统一 `RBAC` 权限接入、演示数据构建与 README / 产品化展示内容完善。

### 4.4 项目亮点

- 将审批、异步执行、发布状态回写、回滚重试、CMDB 同步串成完整发布闭环。
- 支持企业发布平台常见能力，包括审批流、批次发布、灰度发布流程建模与版本治理。
- 具备较强的平台化和产品化特征，不是单一 CRUD 管理后台。

### 4.5 技术栈

- `Django`
- `Django REST framework`
- `Channels`
- `Vue 3`
- `Pinia`
- `Vue Router`
- `Element Plus`
- `Kubernetes Python Client`
- `Docker Compose`
- `Terraform`
- `SQLite`

---

## 5. 面试展开点

- 发布链路：审批流 → 异步执行 → Docker / K8s 发布 → 状态回写 → CMDB 同步
- 技术难点：SSH 远程执行、K8s API 对接、版本回滚建模、RBAC 权限收敛
- 产品价值：把“资源治理、应用发布、运维排障、交付编排”整合到一个平台里

---

## 6. 可直接复制的一段式描述

### 6.1 偏全栈

- 基于 `Django / DRF / Channels + Vue3 / Element Plus` 设计并实现企业级运维与交付平台，覆盖 `CMDB`、应用发布、容器运维、日志审计与 `IaC` 编排，重点落地 `Docker/K8s` 双模式发布、审批流、回滚重试、灰度 / 批次发布及 `CMDB` 自动关联能力。

### 6.2 偏后端

- 负责企业级运维平台后端核心能力建设，基于 `Django / DRF / Channels` 实现应用发布、审批流、异步执行、SSH 远程操作、K8s API 集成与 `CMDB` 同步，支撑 `Docker/K8s` 双模式发布及版本回滚、批次推进等场景。

### 6.3 偏 DevOps / 平台工程

- 参与建设统一运维与交付平台，打通 `CMDB`、应用发布、容器运维、日志审计与 `IaC` 编排链路，支持自研应用 `Docker/K8s` 双模式发布、审批流、回滚重试及发布后资产自动登记，形成较完整的平台工程闭环。

---

## 7. 1 分钟自我介绍稿

### 7.1 通用面试版

- 我这段时间做的核心项目是一个企业内部的 `SxDevOps` 运维与交付平台，技术栈主要是 `Django / DRF / Channels + Vue3 / Element Plus`。
- 这个平台不是单纯的后台管理系统，而是把 `CMDB`、应用发布、容器运维、日志审计和 `IaC` 编排这些能力串起来，做成统一运维门户。
- 我重点负责的是“应用发布”模块，做了 `Docker` 和 `K8s` 两种发布模式，支持审批流、灰度 / 批次发布、回滚、重新执行，以及发布结果自动同步到 `CMDB`。
- 除了发布链路，我也参与了 `K8s` 运维能力增强、Docker 治理和统一 `RBAC` 权限模型建设，所以这块既有后端建模和接口设计，也有前端页面和产品化落地。
- 这个项目让我比较有收获的地方，是把多环境发布、审批闭环和配置项关系同步真正打通，做成一个更接近企业发布平台的完整方案。

### 7.2 偏后端面试版

- 我主要做的是企业级运维平台的后端核心能力建设，基于 `Django / DRF / Channels` 负责应用发布、审批流、异步执行和状态回写这条主链路。
- 在应用发布模块里，我把 `Docker` 和 `K8s` 两类发布目标做了统一建模，同时接入了 `SSH` 远程执行和 `K8s API` 调用，支持灰度 / 批次发布、回滚和重新执行。
- 为了让这套能力能真正落地，我还把发布结果和 `CMDB` 做了自动关联，发布后自动登记应用和目标资源，并建立关系。
- 另外我也补了相关测试、演示数据和文档，保证这套能力既能跑起来，也能用于演示和项目汇报。

### 7.3 偏平台 / DevOps 面试版

- 我参与建设的是一个统一运维与交付平台，目标是把资源管理、应用发布和运维排障做成闭环。
- 我负责的重点是发布平台能力建设，支持公司自研应用在 `Docker` 环境和 `K8s` 集群里的标准化发布，并带审批、灰度 / 批次、回滚和重试能力。
- 同时我把发布过程和 `CMDB` 资产管理打通，让应用、环境、集群和发布记录之间可以形成关系链路，方便后续追踪和治理。
- 这块工作比较体现平台工程的地方，在于不仅要把功能做出来，还要考虑统一建模、可扩展性、权限控制和演示可用性。

---

## 8. 项目经历简历条目版

### 8.1 三条精简版

- 负责 `SxDevOps` 运维与交付平台核心模块建设，基于 `Django / Vue3` 打通 `CMDB`、应用发布、容器运维与日志审计等场景。
- 主导“应用发布”模块落地，支持 `Docker/K8s` 双模式、审批流、灰度 / 批次发布、回滚、重新执行及发布状态跟踪。
- 实现应用发布与 `CMDB` 自动关联，发布后自动登记应用与目标资源并建立关系，形成资源治理到发布交付的闭环。

### 8.2 四条结果导向版

- 设计并实现企业级应用发布模块，统一抽象 `Docker` 与 `K8s` 两类发布目标，支撑多环境发布场景。
- 落地审批流、灰度 / 批次发布、回滚、重新执行与状态回写能力，完善发布闭环。
- 打通发布系统与 `CMDB`，实现应用、环境、集群、发布目标的自动登记与关系同步。
- 参与 `K8s` 运维增强、Docker 治理与统一 `RBAC` 权限建设，提升平台可用性与产品化程度。

### 8.3 招聘平台短条目版

- 企业级运维与交付平台建设，覆盖 `CMDB`、应用发布、容器运维、日志审计与 `IaC` 编排。
- 应用发布模块支持 `Docker/K8s`、审批流、灰度 / 批次、回滚重试与 `CMDB` 自动关联。
- 参与 `K8s` 运维增强、Docker 治理和统一权限模型建设，形成平台化交付闭环。

---

## 9. 30 秒自我介绍稿

### 9.1 通用精简版

- 我最近做的核心项目是一个基于 `Django + Vue3` 的运维与交付平台，里面包含 `CMDB`、应用发布、容器运维和日志审计等模块。
- 我重点负责应用发布这块，支持 `Docker` 和 `K8s` 两种发布模式，并做了审批流、灰度 / 批次发布、回滚和重新执行。
- 同时我把发布结果和 `CMDB` 做了自动关联，让这套能力更接近企业内部真正可用的发布平台。

### 9.2 偏后端版

- 我主要做企业运维平台后端，基于 `Django / DRF / Channels` 负责应用发布、审批流、异步执行和状态回写。
- 在项目里我统一了 `Docker` 和 `K8s` 的发布建模，支持回滚、重试和灰度 / 批次发布。
- 同时把发布流程和 `CMDB` 打通，形成了比较完整的发布闭环。

### 9.3 偏平台版

- 我参与建设了一个统一运维与交付平台，核心是把资源管理、应用发布和运维排障串起来。
- 我负责的重点是应用发布平台，支持 `Docker/K8s` 双模式、审批流、灰度 / 批次和回滚能力。
- 另外还打通了 `CMDB` 资产关联，让平台更贴近企业级发布场景。
